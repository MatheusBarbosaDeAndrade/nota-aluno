import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  resultText: {
    fontSize: 18,
    fontWeight: 'bold',
    textAlign: 'center',
    color: '#e2b24d',
    width: 200,
    margin: '0 auto',
  }
})

export default styles