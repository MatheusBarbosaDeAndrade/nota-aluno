import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: 'bold',
    textAlign: 'center',
    margin: 20,
    color: '#e2b24d'
  }
})

export default styles